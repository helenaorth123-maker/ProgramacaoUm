package Lista2;

public class Exercicio14 {

	public static void main(String[] args) {
		
       boolean chove = true;
        boolean guarda = false;
        boolean carro = true;

        // true && false -> Falso, pois os dois precisavam ser verdadeiros.
        System.out.println(chove && guarda); // Resultado: false

        // false || true -> Verdadeiro, pois pelo menos um é verdadeiro.
        System.out.println(guarda || carro); // Resultado: true

        // !true -> Falso, pois inverte o valor de verdadeiro para falso.
        System.out.println(!chove); // Resultado: false

        // true && (false || true) -> Resolve o parênteses primeiro (true), então: true && true.
        System.out.println(chove && (guardaChuva || carro)); // Resultado: true

        // !(true && false) -> Resolve o parênteses primeiro (false) e depois o NÃO (!) inverte para true.
        System.out.println(!(chove && guarda)); // Resultado: true

	}

}